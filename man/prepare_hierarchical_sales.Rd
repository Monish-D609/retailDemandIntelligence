% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecasting.R
\name{prepare_hierarchical_sales}
\alias{prepare_hierarchical_sales}
\title{Reduce category_week to top N categories + an "other" bucket,
then build a hierarchical (category + total) tsibble ready for forecasting.
Drops the last `trim_weeks` of data, since the final period(s) in this
dataset are typically incomplete (data collection cutoff mid-week),
which would otherwise distort both model fitting and accuracy evaluation.}
\usage{
prepare_hierarchical_sales(category_week, top_n = 10, trim_weeks = 3)
}
\arguments{
\item{category_week}{data.frame from build_category_week_table()}

\item{top_n}{number of top categories to keep individually}

\item{trim_weeks}{number of most recent weeks to drop as incomplete}
}
\value{
a tsibble with an aggregated hierarchy (categories + Total)
}
\description{
Reduce category_week to top N categories + an "other" bucket,
then build a hierarchical (category + total) tsibble ready for forecasting.
Drops the last `trim_weeks` of data, since the final period(s) in this
dataset are typically incomplete (data collection cutoff mid-week),
which would otherwise distort both model fitting and accuracy evaluation.
}
