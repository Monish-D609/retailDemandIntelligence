% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retention.R
\name{build_repeat_purchase_dataset}
\alias{build_repeat_purchase_dataset}
\title{Build a customer-level dataset for repeat-purchase propensity modeling.
Uses ONLY each customer's first order's characteristics as predictors,
since that's the information actually available at prediction time —
using later orders would leak future information (data leakage).}
\usage{
build_repeat_purchase_dataset(order_level, customer_level, review_features)
}
\arguments{
\item{order_level}{data.frame from build_order_level_table()}

\item{customer_level}{data.frame from build_customer_level_table()}

\item{review_features}{data.frame from build_review_features()}
}
\value{
data.frame, one row per customer, ready for modeling
}
\description{
Build a customer-level dataset for repeat-purchase propensity modeling.
Uses ONLY each customer's first order's characteristics as predictors,
since that's the information actually available at prediction time —
using later orders would leak future information (data leakage).
}
